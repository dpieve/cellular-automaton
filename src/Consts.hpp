#pragma once

namespace consts
{
    constexpr static auto width_ = 1024;
    constexpr static auto height_ = 768;
    constexpr static auto framerate_ = 144;
    constexpr static auto title_ = "Cellular Automaton";

    constexpr static auto cell_size_outline_thickness_ = 2.f;

	constexpr static auto cell_size_ = 15.0f;

    constexpr static auto margin_ = 10;

    constexpr static auto num_rows = 30;
    constexpr static auto num_cols = 65;

    constexpr static auto update_ms = 100;
}
